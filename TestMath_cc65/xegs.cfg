# xegs.cfg ? Atari XEGS SMALL (32KB) linker config
# Generated by JumpCS CC65 Backend

SYMBOLS {
    __STACKSIZE__: type = weak, value = $0800;
}

MEMORY {
    ZP:      start = $0082, size = $007E, type = rw, define = yes;
    RAM:     start = $2000, size = $4000, type = rw, define = yes;  # $2000-$5FFF
    BANK00:  start = $8000, size = $2000, type = ro, file = "%O", fill = yes, fillval = $FF;
    BANK01:  start = $8000, size = $2000, type = ro, file = "%O", fill = yes, fillval = $FF;
    BANK02:  start = $8000, size = $2000, type = ro, file = "%O", fill = yes, fillval = $FF;
    FIXED:  start = $A000, size = $2000, type = ro, file = "%O", fill = yes, fillval = $FF;  # Fixed bank
}

SEGMENTS {
    ZEROPAGE: load = ZP,    type = zp;
    BSS:      load = RAM,   type = bss, define = yes;
    DATA:     load = FIXED, type = rw,  run = RAM, define = yes;
    STARTUP:  load = FIXED, type = ro;
    CODE:     load = FIXED, type = ro;
    RODATA:   load = FIXED, type = ro;
    BANK00:   load = BANK00, type = ro;
    BANK01:   load = BANK01, type = ro;
    BANK02:   load = BANK02, type = ro;
}
